<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- Add Surgery Cases smart button to Sale Order form -->
    <record id="view_order_form_surgery_button" model="ir.ui.view">
        <field name="name">sale.order.form.surgery.button</field>
        <field name="model">sale.order</field>
        <field name="inherit_id" ref="sale.view_order_form"/>
        <field name="arch" type="xml">
            <xpath expr="//div[@name='button_box']" position="inside">
                <button name="action_view_surgery_cases"
                        type="object"
                        class="oe_stat_button"
                        icon="fa-heartbeat"
                        invisible="surgery_case_count == 0">
                    <field name="surgery_case_count" widget="statinfo" string="Surgery Cases"/>
                </button>
            </xpath>
        </field>
    </record>

    <!-- Add Informational checkbox to SO line tree view -->
    <record id="view_order_form_line_informational" model="ir.ui.view">
        <field name="name">sale.order.form.line.informational</field>
        <field name="model">sale.order</field>
        <field name="inherit_id" ref="sale.view_order_form"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='order_line']//list//field[@name='price_subtotal']" position="before">
                <field name="is_informational" string="Info Only" optional="show"/>
            </xpath>
        </field>
    </record>

    <!-- Add Informational checkbox to Product form -->
    <record id="view_product_template_form_informational" model="ir.ui.view">
        <field name="name">product.template.form.informational</field>
        <field name="model">product.template</field>
        <field name="inherit_id" ref="product.product_template_form_view"/>
        <field name="arch" type="xml">
            <xpath expr="//div[@name='options']" position="inside">
                <span class="d-inline-flex">
                    <field name="is_informational"/>
                    <label for="is_informational"/>
                </span>
            </xpath>
        </field>
    </record>

</odoo>
